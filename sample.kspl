\Define\#{\K{}}

\#{This is a comment}
\#{Combinatory logic is fun!}

\#{bracket = λx. λy. λz. xzy}
\Define\bracket{\S{\S{\K\S}{\S{\K\K}\S}}{\K\K}}
\#{Magic!}

\Define\par{\bracket{<p>}{</p>}}

\par{A paragraph}
\par{Another paragraph}
\par Actually, this works too, but don't rely on it too much because
it requires that your paragraph doesn't contain specials
\par{So this is the most reliable}

